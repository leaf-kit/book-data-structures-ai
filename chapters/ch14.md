# 14장 문자열 인덱스와 말뭉치

1993년의 논문 하나는 접미사 트리를 버리고 접미사의 시작 위치를 정렬한 정수 배열 하나를 두었다.

## 절

- **14.1** 접미사 배열. 정렬 로그 번으로 만들고 이진 탐색 둘로 센다. 순차 훑기보다 여섯 배에서 스무 배
- **14.2** 무한 n-그램. $n$ 을 정하지 않는 개수 세기. 표보다 맞히고 작다
- **14.3** 긴 중복 찾기. LCP 한 번 훑기로 반복 전부. 소스 코드의 29 퍼센트가 50 바이트 이상의 반복

## 대표 실험

**패턴 세기. 접미사 배열과 순차 훑기** (14.1절)

| 패턴 | 길이 | 개수 | 접미사 배열 us | 순차 훑기 us |
|---|---|---|---|---|
| def  | 4 | 1589 | 119 | 2225 |
| return | 6 | 2000 | 140 | 952 |
| self. | 5 | 3952 | 127 | 950 |
| import numpy | 12 | 0 | 153 | 823 |
| raise ValueError( | 17 | 158 | 171 | 696 |

bench/c14_suffix.py | Python 3.10 표준 라이브러리 소스 1,000,000 바이트, 바이트가 토큰. 접미사 배열 만들기 4.6초 (배가 정렬), 배열 8 MB. 순차 훑기는 C 로 짜인 bytes.count | 반복 5회 중앙값 | 2026-09-09

## 연습문제

- 문제 14.1 (기초) 길이 $10^9$ 의 텍스트에서 접미사 배열의 크기와, 배가 정렬의 정렬 횟수 상한과 각 정렬의 비교 횟수를 앞의 정의 으로 구하라. 8 바이트 정수와 4 바이트 정수의 차이는 얼마인가.
- 문제 14.2 (기초) 길이 20 의 패턴을 길이 $10^9$ 의 텍스트에서 세는 데 비교가 몇 번이고 읽는 바이트가 얼마인지 앞의 정의 로 구하라. 배열이 디스크에 있으면 페이지 접근이 몇 번인가.
- 문제 14.3 (기초) 어휘 5만, 말뭉치 $10^12$ 토큰의 5-그램 표 항목 수의 상한을 앞의 정의 로 적고, 접미사 배열의 메모리와 견줘라.
- 문제 14.4 (기초) banana 의 LCP 배열에서 $L = 2$ 인 반복 구간의 집합을 앞의 정의 로 구하고, 덮는 비율을 앞의 정의 로 구하라.
- 문제 14.5 (응용) `dsai.suffix.build_suffix_array` 의 정렬 키를 정수 하나로 묶는 대신 `np.lexsort` 로 두 열을 정렬하게 바꾸고, 실험 14.1 의 만드는 시간을 견줘라.
- 문제 14.6 (응용) `dsai.ngram.InfiniGram.predict` 를 앞의 정의 의 구간 좁히기로 다시 써라. 짧은 문맥에서 시작해 구간 안에서만 이진 탐색하고, 예측당 시간이 얼마나 주는지 재라.
- 문제 14.7 (응용) `dsai.dupspan.duplicated_positions` 에 앞의 정의 의 첫 출현 남기기를 더하라. 각 반복 구간에서 위치가 가장 작은 것을 뺀 나머지만 돌려주고, 시험으로 첫 출현이 남는지 확인하라.
- 문제 14.8 (응용) 텍스트 둘 $A$, $B$ 를 구분자로 이어 붙여 접미사 배열을 만들고, $A$ 와 $B$ 사이의 가장 긴 공통 부분 문자열을 LCP 로 찾아라. 같은 텍스트 안의 반복은 빼야 한다.
- 문제 14.9 (탐구) 접미사 배열이 디스크에 있을 때 이진 탐색의 각 단계가 접근 하나라는 것을 12.4절 의 잣대로 적고, 위쪽 몇 단계를 메모리에 두는 것이 접근을 얼마나 줄이는지 논하라.
- 문제 14.10 (탐구) 7장의 문서 단위 중복 제거와 이 장의 부분 문자열 중복 제거가 각각 놓치는 중복의 예를 하나씩 들고, 둘을 어떤 순서로 하는 것이 맞는지 논하라.

## 더 읽을 것

- Suffix Arrays: A New Method for On-Line String Searches (1993). 접미사 배열의 원전. 정렬과 탐색이라는 틀과 접미사 트리 대비 메모리 비교가 있다. (14.1절)
- Infini-gram: Scaling Unbounded $n$-gram Language Models to a Trillion Tokens (2024). 토큰 수조 개 위의 무한 n-그램. 접미사 배열을 디스크에 두고 밀리초에 답하는 구현이다. (14.2절)
- Deduplicating Training Data Makes Language Models Better (2022). 문서 단위와 부분 문자열 단위의 중복 제거를 둘 다 한 논문. 접미사 배열 위의 50 토큰 문턱이 14.3 절이다. (14.3절)

정의, 증명, 실험의 분석은 책에 있다.
