# 8장 탐색 트리에서 학습된 인덱스로

2018년의 한 논문은 B-트리의 내부 노드가 하는 일이 키를 위치로 보내는 함수이고, 함수라면 배울 수 있다고 썼다.

## 절

- **8.1** 이진 탐색 트리. 넣는 순서와 높이의 정리, 트립. 평균 깊이 21.7 이 라인 21.7 개다
- **8.2** B-트리. 높이 $ _B n $ 과 차수의 선택. 키 천만 개가 블록 셋
- **8.3** 학습된 인덱스. 오차 상한 조각 선형 모델. 균일한 키에서 B-트리의 6 분의 1, 쏠린 키에서 세 배
- **8.4** 정렬된 배열에 다시 서기. 아이칭거 배치와 정렬된 질의 배치. 순서만으로 세 배

## 대표 실험

**넣는 순서와 높이. 이진 탐색 트리와 트립** (8.1절)

| 넣는 방식 | 키 수 | 높이 | 평균 탐색 노드 | 2 log2 n |
|---|---|---|---|---|
| 무작위 순서 BST | 100000 | 44 | 21.7 | 33.2 |
| 정렬 순서 BST | 10000 | 10000 | 4953 | 26.6 |
| 정렬 순서 트립 | 10000 | 28 | 16.0 | 26.6 |
| 무작위 순서 트립 | 100000 | 39 | 21.2 | 33.2 |
| 완전 균형 (하한) | 100000 | 17 | 15.6 | 16.6 |

bench/c08_height.py | 무작위 40비트 정수 키. 탐색은 들어 있는 키 2,000개. 정렬 순서는 10,000개만 | 반복 1회 중앙값 | 2026-09-09

## 연습문제

- 문제 8.1 (기초) 키 백만 개를 무작위 순서로 넣은 이진 탐색 트리의 기대 깊이를 앞의 정의 로 구하고, 완전 균형의 높이와 견줘라. 노드 하나가 라인 하나이면 탐색 하나에 라인 몇 개인가.
- 문제 8.2 (기초) 키 십억 개, 8 바이트 키에서 라인 64 바이트와 페이지 4 KB 의 차수와 높이를 앞의 정의 로 구하라. 내부 노드는 각각 몇 MB 인가.
- 문제 8.3 (기초) 조각 2,000 개, 오차 상한 64 의 학습된 인덱스의 메모리와 탐색 비교 횟수를 앞의 정의 로 구하고, 같은 키 백만 개의 차수 64 B-트리와 견줘라.
- 문제 8.4 (기초) 키 $2^30$ 개에 질의 $2^16$ 개를 정렬해 찾을 때 탐색당 새 라인 수를 앞의 정의 로 구하라. 정렬 안 하면 몇 개인가.
- 문제 8.5 (응용) `dsai.bst.Treap` 에 삭제를 더하라. 지울 노드를 회전으로 잎까지 내린 뒤 떼어 낸다. 지운 뒤에도 두 불변식이 지켜지는지 시험으로 확인하라.
- 문제 8.6 (응용) `dsai.btree.StaticBTree` 의 블록 안 이진 탐색을 순차 비교로 바꾸고, 차수 8 과 16 에서 실험 8.2 의 시간이 어떻게 바뀌는지 재라. 어느 차수부터 이진 탐색이 이기는가.
- 문제 8.7 (응용) `dsai.learned.PiecewiseLinearIndex` 를 앞의 정의 대로 두 층으로 만들어라. 조각 시작 키 위에 같은 인덱스를 세우고, 조각 찾기의 비교 횟수가 얼마나 주는지 재라.
- 문제 8.8 (응용) `dsai.layout.batch_search` 에서 정렬한 질의를 앞 질의의 위치부터 갤로핑 (보폭을 두 배씩 늘리는 탐색) 으로 찾게 바꾸고, 실험 8.4 와 견줘라.
- 문제 8.9 (탐구) 학습된 인덱스에 삽입을 더하려면 어떤 불변식을 어떻게 완화해야 하는지 논하라. 오차 상한을 지키면서 조각 하나만 다시 만드는 조건은 무엇인가.
- 문제 8.10 (탐구) 문자열 키에 학습된 인덱스를 세우려면 키를 실수로 바꿔야 한다. 앞 8 바이트를 정수로 읽는 방법의 오차 상한이 왜 커지는지 앞의 정의 로 설명하라.

## 더 읽을 것

- Organization and Maintenance of Large Ordered Indexes (1972). B-트리의 원전. 디스크 페이지 위의 인덱스라는 가정과 분할, 병합이 다 있다. (8.2절)
- The Case for Learned Index Structures (2018). 학습된 인덱스를 제안한 논문. 내부 노드가 누적 분포 함수라는 관찰과 두 층 모델의 실험이 있다. (8.3절)
- The PGM-index: A Fully-Dynamic Compressed Learned Index with Provable Worst-Case Bounds (2020). 오차 상한이 보장되는 조각 선형 학습 인덱스. 최악의 경우 경계와 삽입이 있다. (8.3절)
- Array Layouts for Comparison-Based Searching (2017). 정렬 배열의 네 배치를 여러 기계에서 잰 논문. 아이칭거와 미리 읽기의 실측이 8.4 절의 근거다. (8.4절)

정의, 증명, 실험의 분석은 책에 있다.
